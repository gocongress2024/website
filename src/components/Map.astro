---
import MapComponent from "./Map.tsx"
import places from "../data/mapPlaces.json"

interface Props {
  className: string
}

const { className } = Astro.props
---

<MapComponent class={className} places={places} client:only="solid-js" />
